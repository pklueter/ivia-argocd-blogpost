apiVersion: v1
kind: Service
metadata:
  name: iviaconfig
  annotations:
    traefik.ingress.kubernetes.io/service.serversscheme: https
spec:
  ports:
    - port: 443
      targetPort: 9443
      name: iviaconfig
      protocol: TCP
  selector:
    app: iviaconfig
